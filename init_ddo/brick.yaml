name: init_ddo
description: A brick to create data domain object
version: 0.1.0+1
environment:
  mason: ">=0.1.0-dev.50 <0.1.0"
vars:
  sub_directory:
    type: string
    description: sub_directory name
    default: default
    prompt: sub_directory name?
  repository_name:
    type: string
    description: repository_name name
    default: default
    prompt: repository_name name?
