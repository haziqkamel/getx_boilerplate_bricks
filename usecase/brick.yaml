name: usecase
description: A new brick to create usecase
version: 0.1.0+1
  mason: ">=0.1.0-dev.50 <0.1.0"
vars:
  sub_directory:
    type: string
    description: sub_directory name
    default: default
    prompt: sub_directory name?
  usecase:
    type: string
    description: usecase name
    default: default
    prompt: usecase name?
